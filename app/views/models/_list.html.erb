<script type="text/template" id="model_list">

	<%= render 'shared/navigation' %>

  <table class='table table-striped table-bordered'>
    <thead>
			<tr>
				<th>Name</th>
				<th></th>
				<th></th>
			</tr>
    </thead>
    <tbody>
			<@ _.each(models, function(model) { @>
					<tr>
							<td><@= model.get('name') @></td>
							<td><a href="#models/<@= model.get('_id')['$oid'] @>/edit" class="glyphicon glyphicon-pencil"></a></td>
							<td><a href="#models/<@= model.get('_id')['$oid'] @>" class="glyphicon glyphicon-trash delete-asset-type" data-confirm="Are you sure?"></a></td>
					</tr>
			<@ }); @>
    </tbody>
  </table>

</script>